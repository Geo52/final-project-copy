<!DOCTYPE html>
<html>
  <head>
    <title>Assist</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
      <div class="container-fluid">
        <a class="navbar-brand" href="/courses">Assist</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon">s</span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/courses">Home</a>
            </li>
            <% if user_signed_in? %>
              <li class = "nav-item">
                <%= link_to "My Courses", registered_courses_path(current_user), class: "nav-link" %>
              </li>
            <% end %>
            <li class="nav-item">
                <li><a class="nav-link" href="/users/sign_in">Sign in</a></li>
                <li><a class="nav-link" href="/users/sign_up">Sign up</a></li>
                <li><%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-link" %></li>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <% if notice != "" and notice != nil %>
      <p class="notice"><%= notice %></p>
    <% end %>
    <% if notice != "" and notice != nil %>
      <p class="alert"><%= alert %></p>
    <% end %>
    <div class="container-fluid">
      <%= yield %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </body>
</html>
